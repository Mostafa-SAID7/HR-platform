namespace HR.Analytics.Application.Mappings;

/// <summary>
/// Centralized mapping configuration for Search Result DTOs.
/// Organized by aggregate to follow SOLID principles.
/// SearchResultDto is a generic wrapper, so it doesn't have specific mapping logic.
/// </summary>
public static class SearchResultDtoMappingConfiguration
{
    // SearchResultDto<T> is a generic wrapper class with no specific domain mapping needed
    // It's used to wrap search results from various sources
}
